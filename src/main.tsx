import React from 'react'
import ReactDOM from 'react-dom/client'
import FinelessDashboard from './FinelessDashboard'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FinelessDashboard />
  </React.StrictMode>
)
